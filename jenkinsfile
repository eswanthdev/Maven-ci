pipeline {
    agent none
        stages {
            stage ('First_stage') {
           steps {
               sh 'git clone https://github.com/eswanthdev/Maven-ci.git'
            }  
          }
            stage('second stage') {
            steps {
                sh 'mvn --version'
            }
        }
       } 
